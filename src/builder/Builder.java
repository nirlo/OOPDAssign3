package builder;

public interface Builder<T> {

	/**
	 * So Each of the builders will have to implement this interface. I'll let you go through and decide which 
	 * methods need to be in here. I mostly noticed that I needed to add this so I could make the DAOs 
	 * generic. I'm sure you'll get it
	 * 
	 * 
	 */
}
